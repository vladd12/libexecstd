project(cuda LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

set(INCLUDE_CUDA_DEPENDS_PATH ${INCLUDE_PATH}/${PROJECT_NAME})
message(STATUS "CUDA library include path: ${INCLUDE_PATH}")
